id: medusa_weights_to_a_matrix
label: Weights to A Matrix
category: '[Medusa]'

templates:
  imports: from gnuradio import medusa
  make: medusa.weights_to_a_matrix()

inputs:
- label: weights
  domain: message
  optional: true

outputs:
- label: A
  domain: message
  optional: true

file_format: 1
