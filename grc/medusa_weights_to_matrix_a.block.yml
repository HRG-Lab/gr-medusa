id: medusa_weights_to_matrix_a
label: Weights to A Matrix
category: '[Medusa]'

templates:
  imports: from gnuradio import medusa
  make: medusa.weights_to_matrix_a()


inputs:
- label: weights
  domain: message
  optional: True

outputs:
- label: A
  domain: message
  optional: True

file_format: 1
